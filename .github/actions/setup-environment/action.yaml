name: setup-environment
description: Python environment setup

inputs:
  python-version:
    description: The Python version to install and use
    default: "3.12"
    required: false
  uv-version:
    description: The uv version to install and use
    default: "0.11.19"
    required: false

runs:
  using: composite
  steps:
    - name: Setup uv and Python ${{ inputs.python-version }}
      uses: astral-sh/setup-uv@v10
      with:
        version: ${{ inputs.uv-version }}
        python-version: ${{ inputs.python-version }}
        enable-cache: true
